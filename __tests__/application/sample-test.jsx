/* globals describe, it */
/* jshint expr: true */
'use strict';

var expect = require('chai').expect;

describe('sample test', function() {
    it('demonstrates that tests run', function() {
        expect(1 + 1).to.equal(2);
    });
});
